{
  "name": "script-runner",
  "version": "1.0.0",
  "description": "Script runner",
  "main": "src/main.js",
  "build": {
    "extraFiles": [
      "./assets/**"
    ]
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-packager ./ ScriptRunner --platform=darwin"
  },
  "devDependencies": {
    "electron": "^7.0.1",
    "electron-packager": "^14.1.1"
  },
  "dependencies": {
    "electron-store": "^5.1.0",
    "sortablejs": "^1.10.1"
  }
}
